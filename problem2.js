function fibbonaci(integer) {
    let currentNum = 1;
    let previousNum = 0; 
    console.log(previousNum)
    for (let i=2; i <= integer;i++) {
        newNum = previousNum + currentNum
        previousNum = fibonacciNumber
        currentNum = newNum
        console.log(currentNum)
    }
}
fibbonaci(10);
/*previous number plus current number equals to next number */